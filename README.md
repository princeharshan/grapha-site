# grapha.ai

The Grapha homepage. **Generated** — do not edit these files here.

The sources live with the app, in `site/concepts/`; `mksite.py` builds
`index.html` and `styles.css` and `publish.py` copies the result into this
repo. An edit made here is overwritten by the next publish.
